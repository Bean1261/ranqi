<?xml version="1.0" encoding="UTF-8" ?>
<mapper namespace="com.weichai.ranqi.mapper.LedgerMapper">

    <!-- 插入 Ledger -->
    <insert id="insertLedger" parameterType="com.weichai.ranqi.entity.Ledger">
        INSERT INTO ledger (
        inspection_type, check_time, rectification_deadline, hazard_severity,
        issue_category, sub_category, detailed_category, department, hazard_area,
        person_in_charge, sub_area, detailed_area, issue_object, issue_mode,
        l_value, e_value, c_value, d_value, before_rectification_photo,
        after_rectification_photo, issue_description, is_first_discovery,
        team_leader, deduction_score, inspector, analysis_and_measures,
        rectification_finish_time, assessment_person, assessment_amount,
        create_time, update_time
        )
        VALUES (
        #{inspectionType}, #{checkTime}, #{rectificationDeadline}, #{hazardSeverity},
        #{issueCategory}, #{subCategory}, #{detailedCategory}, #{department}, #{hazardArea},
        #{personInCharge}, #{subArea}, #{detailedArea}, #{issueObject}, #{issueMode},
        #{lValue}, #{eValue}, #{cValue}, #{dValue}, #{beforeRectificationPhoto},
        #{afterRectificationPhoto}, #{issueDescription}, #{isFirstDiscovery},
        #{teamLeader}, #{deductionScore}, #{inspector}, #{analysisAndMeasures},
        #{rectificationFinishTime}, #{assessmentPerson}, #{assessmentAmount},
        #{createTime}, #{updateTime}
        )
    </insert>

    <!-- 根据ID获取Ledger -->
    <select id="getLedgerById" resultType="com.weichai.ranqi.entity.Ledger">
        SELECT * FROM ledger WHERE id = #{id}
    </select>

    <!-- 获取所有Ledger -->
    <select id="getAllLedgers" resultType="com.weichai.ranqi.entity.Ledger">
        SELECT * FROM ledger
    </select>

    <!-- 更新Ledger -->
    <update id="updateLedger" parameterType="com.weichai.ranqi.entity.Ledger">
        UPDATE ledger
        SET
        inspection_type = #{inspectionType},
        check_time = #{checkTime},
        rectification_deadline = #{rectificationDeadline},
        hazard_severity = #{hazardSeverity},
        issue_category = #{issueCategory},
        sub_category = #{subCategory},
        detailed_category = #{detailedCategory},
        department = #{department},
        hazard_area = #{hazardArea},
        person_in_charge = #{personInCharge},
        sub_area = #{subArea},
        detailed_area = #{detailedArea},
        issue_object = #{issueObject},
        issue_mode = #{issueMode},
        l_value = #{lValue},
        e_value = #{eValue},
        c_value = #{cValue},
        d_value = #{dValue},
        before_rectification_photo = #{beforeRectificationPhoto},
        after_rectification_photo = #{afterRectificationPhoto},
        issue_description = #{issueDescription},
        is_first_discovery = #{isFirstDiscovery},
        team_leader = #{teamLeader},
        deduction_score = #{deductionScore},
        inspector = #{inspector},
        analysis_and_measures = #{analysisAndMeasures},
        rectification_finish_time = #{rectificationFinishTime},
        assessment_person = #{assessmentPerson},
        assessment_amount = #{assessmentAmount},
        create_time = #{createTime},
        update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 删除Ledger -->
    <delete id="deleteLedgerById">
        DELETE FROM ledger WHERE id = #{id}
    </delete>

</mapper>
